# Ví dụ file .env — copy thành .env và điền thông tin thật

# Model paths (tùy chọn — mặc định tự tải)
# VEHICLE_MODEL_PATH=yolov8n.pt
# PLATE_MODEL_PATH=plate_detector.pt

# PaddleOCR (tùy chọn)
# paddleocr lang: vi_en, ch, en, ...
OCR_LANG=vi_en

# Paths
# DATA_DIR=data
# OUTPUT_DIR=outputs
